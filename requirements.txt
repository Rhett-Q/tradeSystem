pandas>=2.0.0
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
pydantic>=2.0.0
eval_type_backport>=0.2.0

# xtquant ships with MiniQMT (not on PyPI).
# Install path: D:\gjqmt
# Python: prefer 3.9/3.10/3.11 (some QMT cp38 builds are broken).
# Setup:
#   scripts\setup_env.cmd
# Clean old Python (keeps 3.9/3.10/3.11):
#   scripts\clean_old_python.cmd          preview
#   scripts\clean_old_python.cmd --yes    uninstall
# Web:
#   scripts\run_web.cmd
